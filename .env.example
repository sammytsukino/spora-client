# Vite: only variables prefixed with VITE_ are exposed to the client.

# Backend API base (must include /api). Local:
VITE_API_BASE_URL=http://localhost:4000/api

# Production example (your Render URL):
# VITE_API_BASE_URL=https://spora-api.onrender.com/api
